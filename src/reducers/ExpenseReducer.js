const ExpenseReducer = ({ state, action }) => {
    switch(action.type){
        case 'ADD_EXPENSE':
            return 
        case 'REMOVE_EXPENSE':
            return 
        default:
            return state
    }
}

export default ExpenseReducer;